# 12 个性能测试：合规优化映射

整改日期：2026-07-15

旧报告中的 67.74 分来自编译期执行整个程序，不能作为代码生成性能成绩。本文件不保留该分数，后续数据必须由实际生成的 RISC-V 汇编运行得到。

| 测试方向 | 合规实现位置 | 后续工作 |
|---|---|---|
| 常量计算、常量表达式链 | IR 常量传播 | 补充边界值与溢出测试 |
| 死代码 | DCE、不可达块删除 | 加强跨块活跃性分析 |
| 复制传播 | mem2reg、Phi 简化 | 消除冗余 move/load/store |
| 公共子表达式 | GVN/CSE | 扩展安全的跨块值编号 |
| 代数化简 | IR 标量优化 | 增加强度削减并保护除零/溢出语义 |
| 尾递归 | 当前按普通调用生成 | 在 IR 上实现可证明安全的尾调用消除 |
| 大循环、综合用例 | CFG + 跨基本块寄存器分配 + Phi 合并 | 改进 spill、LICM 和循环布局 |
| 图和矩阵类计算 | 通用 IR 与 RV32IM 后端 | 循环优化和访存优化 |
| 全局常量传播 | 全局初始化与 IR 常量 | 增加保守的过程间只读分析 |

## 取舍原则

1. 正确性和完整编译链优先于性能分。
2. 优化必须对通用程序结构生效，不针对某个测试的最终答案。
3. 编译时间应主要由源码和 IR 大小决定，不能随待生成程序的运行次数增长。
4. 性能结论必须同时记录测试输入、生成汇编、执行环境和测量值。

当前不声明新的 12 点性能总分；完成 RISC-V oracle 和基准复测后再更新。
